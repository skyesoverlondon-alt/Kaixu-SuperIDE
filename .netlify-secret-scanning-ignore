# Netlify secret scanning ignore patterns
# These are public endpoints / placeholder values — not secrets.

# Worker URL is a public HTTPS endpoint, not a credential
kaixusi.skyesoverlondon.workers.dev

# Example/template files — intentionally contain placeholder values
.env.example
worker/wrangler.toml
